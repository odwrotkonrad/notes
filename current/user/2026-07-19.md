# 2026-07-19

## Next
- go over che e2e log and fix issues
- che backup create|restore|list, remove backup restore from uninstall
- ontoRepoLocal, ontoRepoCheckedIn

## Current
- che e2e

## Later

## Done

## Other


each scenario should have status: implemented / todo


during plan we should be creating scenarios
  but not always


define test types



---
before che execution, when invoking subcommand i
  want to see che config log

  config(delta): and print config key value pairs
  that are different than default and with a source
  (env, config, cli-flag)

  in debug mode i want to have additional line
    config(debug,all): with all the config options
    along with default values
---


---
for init-remote-sources i want to have log lines:
  init-remote-sources(detectedRemote): profile=<profile> <dependency>

  init-remote-sources(cloneRemote): <git-url> -> <os-path>
  init-remote-sources(updateRemote): <git-url> -> <os-path>
  init-remote-sources(noopRemote): <git-url> -> <os-path>

i want to change the dir sources are located to remote-sources
  debug init-remote-sources(noop): spec: no update required /Users/ko/.cache/che/sources/gitlab.com-konradodwrot-workspace

---
having


---
prune-links should be renamed to prune-broken-links

in discovered line:

discover-profiles(discovered): desktop/macos: [prune-links(delta=0,all=56),make-dirs(delta=0,all=106),make-links(delta=0,all=127),make-copies(delta=0,all=7),render-templates(delta=0,all=15),run-scripts(delta=18,all=18)]

i want to have ops: <array of ops> and workingDirectory: <dir>
  and workingDirectory should be first key


---

dry run in configs in macos tart is breaking too often, let's use gitlab darwin worker instead


I want to implement e2e test in che similar to how it's done in configs
  it should have 4 profiles: 1 dropped due condition, 1 autodiscovered with no condition, 1 with condition, 1 remote (using local repo ref)
    every op should be defined in included profiles
    these commands should be tested:
      che os mutating commands should be tested
        and in between uninstall should be called
        che run should run first, uninstalled and rest of commands then standalone
        whole flow should be one job
        and each operation should be preceded with dry run
      basic assertions against a system


---
can we trigger oci ci image build on every che release?
---

if i create e2e test that will invoke a cmd, and test will be written in go,
  how easy is to count the coverage?


I want to rewrite e2e test to go, and i want to have to jobs
  dryrun + debug=true,
  normal run with debug=false
