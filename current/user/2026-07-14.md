# 2026-07-14

## Next
- sandbox repo needs github token


## Current

## Later

## Done

## Other

automatic dep bumps won't help me, with 0.0.X
  Id need 1.0.0 release with git flow improved
    and i would be deploying a lot of major versions.. so lets not do it

che

dir map?
  options?


makeLinks
  it just links all as is with / prepended


--- this syntax in che.yml:

makeLinks:
  - {source: HOME/**, dest: 's#^HOME#$HOME#'}

  is not friedly.

  i must somehow declare replaces for dirs
  HOME is wrong dir name becuase on some filesystems it's just home (case insensitive)
  so...
    what would be better?
    _home ?
    brainstorm ideas with me

--- i want to inspect db items easily

--- i want to inspect metrics easily
      grafana dashboard for che
      can you implement a grafana dashboard for inspecting che metrics?


the services key seem bad

i want to document all metrics for che
  i want to store metrics definitions in a single che file (if not already done)

maybe tracing can be implemented
  in code id like to see functions being invoked and remotes being fetched and other external interfaces being called

done:
- traces che
- che.yml config update _home -> $HOME
- log/metric/trace links in dashboard
- remove services subcommand

current:
- i want otel collector to be deployed to kind cluster
- history completion arrow bottom arrow up
- i want to control profiles execution order via profiles key in che


next:

- split configs into plugins
- test from configs should be moved to che as integration/e2e

// it should not use fzf, it must use zsh completion system to its fullest potential according to man zshcompsys and zshcompwid, I need to verify changes before shipping

// i think history_complete_word is hinting history words not whole lines, but i may be wrong, we may need own completion widget


I want to start managing identity of a sandbox via iac
  i want to create these identities:
    gitlab - for group
      permissions: write to group and read to group, no deletions
    gcp secrets manager - for secrets to be loaded into a session e.g. credentials to gitlab

    when i start a session it should get a gcp identity with read permissions to secrets manager to read secrets it has permissions to

    i want to create subgroup in konradowdrot, "restricted" that agents would not have write access to
      it can be terraform based, all sensitive values must be marked sensitive

---
i want to add this group to index and to clone in configs
  i also want to support multiple groups in the workspace config so both of these groups will be cloned and indexed
i want CI for this repo but job log should be made private (for all repos in restricted group)
i want to mask secrets
and all terraform sensitive values must be marked sensitive

I want to modify my workspace so it's on gitlab dir, and we should add restricted repos here under r/ key so infra becomes r/infra, konradodwrot subgroups and repos should be without prefix

i want to ensure that index script indexes all groups up to gitlab


---

i want to move sandbox to restricted group

---
i want to orgnize tf code into modules e.g. gitlab gcp github and invoke modules from top level tf code
can you verify sandbox gitlab identity have no write access to restricted repos?
and can you verify sandbox  gitlab identity can't read secrets/job output?


---
now i want to wire credentials from gcp to sandbox, it will require changes in config (virt type of machine would use gcp, and host would use op secrets), also verify are there any other missing gcp secrets that could be needed in the gcp secrets manager? which secrets are currently rendered in sandbox?


---

can we build the image inside a cluster, and maybe share host dir with cluster so
  image is in there right away? and where are we exporting the image?
