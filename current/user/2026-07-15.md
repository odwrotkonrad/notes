# 2026-07-15

## Next

## Current
- permission issues claude
- che dry-run=true should be supported flag

## Later
- ensure lines
-

## Done

## Other

rename targets so they are not run-*

current:
---
prerelease for che so i do not need to merge
why im installing deps of che in configs, isnt that shipped together
che:
  log - archive -> backup
id like to not publish time in log on each line, it clutters the output
dry run should be announced before che command once not in (...) log lines, to declutter output
---

next:
---
Sections:
  in log it should not have these --- --- markers anymore, instead i want it to be bold with "##[>] sect" as section begin marker and "##[<] sect" as section end marker

Improve log:


---

completion:
  cd <thing>
    i want thing to look up in order:
      current dir
      dir stack
      contents of current dir (recursively) (limit to 10 results)



---
-------------------- BEGIN 00-ci-deps.zsh            --------------------
go: already installed
che: keeping dev (newer than pin 0.0.52)
lefthook: already installed (2.1.9)
yq: already installed (4.53.3)

improve the log,

first verb (install) then subject (go) then additional information
id imagine perfect log:

## Installing binaries
### Go
<what is actually happening>
<result>
### Che
<what is actually happening>
<result>

etc.


---
profile after profile
che all --stages=all


as i conversate with claude im technically writing a spec that could be forgotten

spec directory with features


I write user story, you write behavior gherkin


spec could be a convention
  CLAUDE.md discouraging editing files


I want to have this spec directory structure:

  spec/<feat>/story.md behavior.md

  i currently have one story with no behavior.md

  your job is to write behavior.md with 3 most problematic implementation details you encountered in gherkin


behavior.md - documented problematic scenarios during implementation in gherkin
story.md - user story high level of a feature


maybe it should also search within the directory stack for an item

---
in +1 +2 groups
  we will only want to show items that match this group segment not the whole
  for pwd and for stack
    so if it would match a dir from PWD and it would not match subdir from PWD+1 it will not be shown in a PWD+1 group
  when i type no search pattern i do not want to show +1 and +2 groups at all
---


---
id like to add 2 additional groups similar to the others
..
and ../*
  in ../* id like to not show repetition candidates from *

both capped to 10 elements

these should appear after pwd+1 pwd+2 groups, and similarly have non-hidden hidden grps


---
i want to write file completion similar to how my directory completion works
  it should be capped at the same level, in a way that in the leaf we're not searching for directories but only for files

  so * will show only files with dirs excluded in pwd */* in directories in pwd and dirs in it should be excluded etc.

  can you write a short user story and some basic behavior.md in zsh_completion_files?


---
some issues with searching within dir stack


---
some commands can take dir and file as input, we could have common completer too
  e.g. code would use both dirs and files completions

---
we have 3 similar completers: dirs, files, and dirs+files, can you take a look and try to reduce code duplication?

// most visited dirs?
