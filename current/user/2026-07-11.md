# 2026-07-11

## Next
- agent per repo
- telemetry in sandbox
  - collector deployed in the cluster


## Current

## Later

## Done

## Other




--- STAGE 1
I want to make changes to my repos

for each repo i want to create an agent in it's local ~/.claude/agents directory
I want to have 2 agents:
  read-only for scoping, planning, reviewing changes (model: fable)
  read-write for executing plans (model: sonnet)


default model for each repo is read only agent and it have additional tool and permission to invoke write agent.
    tools: Agent(<rw-agent>)
  generally bash can be read for exploration tasks, so do not block any tools, but in prompt specify that this agent should not make any write changes.

each agent should have clearly defined concise, terse purpose in description in Purpose: <>.

ro agent should not have git skills loaded, it should have minimal context loaded - knowledgable about repository
  it can have some context loaded about context he is in, so purpose of sibling repositories

each agent should have constraint(or recommendation) - it should be invoked with pwd set to repo path. If not it should report to invoker agent so it can be reinvoked with repo path (at start of a task)

permissions should be set to bypassPermissions
it should be che based and these agents should only render in correct dir if this is "virt" type of system.

use agents local settings checked to repository set to default rw agent, but this should be only loaded on virt type of machines. (use template and che for this)

these agents should have
  memory: local
  (it should not be checked into vcs)

read-write agents should have instructions to proactively save to memory:
similar to this:

"""
Update your agent memory as you discover codepaths, patterns, library
locations, and key architectural decisions. This builds up institutional
knowledge across conversations. Write concise notes about what you found
and where.
"""

also add that, (most crucial part): difficulties that surfaced during implementation, should be noted in memory under own heading in memory 'obstacles' or similar.

each agent should have in instructions: path to repo. (and it should be invoked with pwd set to this path), it can be absolute, in this case use template and che.


--- STAGE 2:
each subgroup should include agents from child repos in their own ./.claude/agents directory.
each subgroup should get its own agent (ro) that have access to other agents within group.
it should be noted that for planning and exploring, scoping within repository the ro agent of repo should be used, with pwd set to repo direcotry
  and for implementation rw agent should be used for

  and for multirepo changes multiple plan agents should be spawned to help with planning, and multiple implementation rw agents should be spawned during implementation

it should be default agent for this subgroup.
(only if virt, use templating and che for this)


--- STAGE 3:
each repository should have "verify" makefile target that should run automated checks that agents can use to verify changes.

each subgroup should include this target so their agents can verify multiple repositories at once, (subgroups gets their own makefiles with targets run-verify-<repo>)



--- AD 1





---
