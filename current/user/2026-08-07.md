# 2026-08-07

## Next
- make sandbox actually usable
- write a spec for this
- che profiles could have verify scripts at the end




## Current
- rework linux installs to some more robust package manager, lets use apt, and remove linuxbrew
    ideally make it somehow extensible so i can add other package managers later if package names can overlap
- refactor auth
    - all as iac


## Done
- make a formulae versioned
- move to github with a tap
- use brew to install che on darwin

## Later Maybe

## Other


completions:
  given i typed a command and i press down i do not want to see this duplicated words:
  also add a gherkin for this



- refactor auth
    - all as iac

  i want to control identity of a sandbox as an iac, make a auth module in iac project
    it will be able to write to onepassword and to gcp secrets manager and serve as a control point
      it should create identites, tokens and permissions
      sandbox will get access to gcp secrets manager to fetch all identity
      this gcp identity will be injected from host, and it will be stored in one password

    i want this permissions:
      gitlab: can push to non protected branches, and create MRs to konradodwrot group
      github: read access
      gcp: read secrets
        sandbox should get own management group in this management group there should be project auth within it
          within this management group there should be dev management group (for now we should give it read access until we design something cool)
      onepassword: it wont get one password identity


    scope:
      iac new auth module
      sandbox gcp auth injection from host+onepassword (host calls 1p)
      sandbox other auth init from gcp
      gherkin scenarios on each of repos

    there will be new vault in OP SandboxProgrammaticAccess
      host will get write access to it



zsh for packages.yml is limiting
  I want this functionality to be moved to che as command
    che packages install
    extend spec with a list of packages to install-packages (in profile as include.installPackages: [...])
    packages.yml location could be configurable, with a builtin (what we have)
      packages-override.yml could be specified as an option or che could look for it in it's config directory

    default behavior is:
      if package was installed by selected package manager, and version is not specified do not update
      if version was specified and is not matching ensure version matches by reinstalling

    options are:
      --update (if package is installed via package manager and version is not specified update package, having it was installed by selected package manager, if version is specified check if matches, if not reinstall to match)
      --if-missing (only install if missing, regardless of location / what package manager was used)

    i want to have another subcommand:
      che packages check
        its purpose is to be called after install and it would give information:
          WARN:
            shadowed by (if it was installed but is shadowed by binary with the same name in different path location)
            more-recent-available (accounting for binaries too)
            multiple-present (and print info where these are)


also extend e2e tests with new commands
  in e2e i want to test:
    upgradable against package manager and binaries
    shadowed / not shadowed
    multiple present
    single present
    not present at all
    installation failure e.g. missing package in package manager or wrong url
    missing package in config


  it can be new e2e test to test che packages subcommands

in the meantime packages.yml in configs changed, can you account for this changes?

i dont like that we're shipping more than one binary with che
  could be one

id like to maintain a default config file for che a a source of default values
  we could output it as a subcommand
    che config default
