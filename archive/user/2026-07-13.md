# 2026-07-13

## Next

## Current

## Later

- for deleting stale stuff i need integration test
- split che testing from config testing

## Done

## Other


Imagin completion that is programmatic and have history layer on top for ordering


che
	domain remodeling
	data store
	che spec:
		configurable core folders, HOME, root
	options / config in spec on top level
	options / config in config file

	full XDG compliance


monitoring config kind

expose telemetry che


--- can you take a look at this repo and check for any structs that are not defined in models.go files? are there any?

run it in parallel claude instance per package


---che  models rethought

ProfileRecipe // assemble the profile, contains defs from che.yml
  Include
    Command (LinkCommand, CopyCommand)

  Interface:
    makeProfile() // resolve profileRecipe data and create Profile

Profile // resolved profile, ready to be installed onto OS
  Commands<type> list of commands


Operation <Generic>
  files
  options

  Interface:
    prepareOperation()
    executeOperation()

Spec
  Source
  ProfileRecipes list
  Profiles list

  Interface:
    makeProfiles()

    prepareSpec(source) // initializes spec with empty profileRecipes and Profiles
    prepareProfileRecipes() // prepares profile recipes
    prepareProfiles() // for each profile recipe resolve data and prepare a profile to be executed

Che
  Specs <list>
  Config

  Interface:
    prepareConfig()
    prepareSpecs(Sources list)


// Plugin changes:
  profile like base: could now be remote string e.g. "@https://gitlab.com/konradodwrot/workspace.git/che.yml::gitlabGroup" and plugin concept could disappear, in favor of remote references



---
"plugin concept"


func (s Source) Validate() error   // non-empty; remote: parseable git URL; filepath: legal path shape
  and is valid che.yml spec


func (s Source) IsRemote() bool    // @ prefix
func (s Source) ResolveDir(repoRoot, home string) (string, error)
  these helper methods should not be part of core interface



func (s *Spec) PrepareSpec(src spec.Source) error  // validate + ResolveSourceDir + locate che.yml
  ResolveSourceDir -> PrepareDirectory (if remote there clone should happen, if local, just validate it's correct)



  type ProfileRecipe struct {
	Name    string
	Source  Source         // stamped at parse: the containing che.yml's source reference

    Name and Source are the same thing
      when using name of remote profile, we should get some helper method to infer name from remote name

      so rename it to Source


--
  Dir -> Directory and move it to options

  	Dir     string         // stamped at parse: the spec's effective directory (profile options.Directory overrides it)

type ProfileOptions struct {
	ExecIf       []string
	AutoDiscover bool
	Directory    string // anchor dir override for this profile
}

ExecIf can also be option on spec Options
same for AutoDiscover
Debug can be option settable on profile

let validateSpec be top level only though



---

func PrepareOptions() (config.Config, error)
  -> rename config.Config so this object is called Options


func SourceProfileName(s Source) string   // local: s itself; remote: the ::<profile> suffix
  -> getProfileName

and let these functions be methods


func (s Source) IsRemote() bool           // has a repo part (@ prefix or <dir>::)

  getSourceType with remote | filesystem, and checking is inline



func (s Source) Validate() error          // non-empty; remote: parseable git URL; filepath:
  rename to -> isValid


Sources should a top level struct
  it should contains a list of sources


Sources
  since each operation needs a source of info (spec) and directory..

  we should have a Sources top level struct
    this struct should have props:
      SourcePath (local or remote)
      SourceDirectory (if remote initialized in some XDG location)

      and it should have method prepareSource(); isValid() getSourceType()


2 types of sources
  definition
  workingDirectory

so source will be
  Source
    DefinitionURI
    WorkingDirectoryURI

  and to not overcomplicate it, lets have methods:
    prepareSource() // noop for local, for remote clones to XDG loc
    getLocalWorkingDirectory() // workingDirectoryURI for remotes will be remote file, we can programatically get dir of cloned repo


they way we can declare profile with remote str url is a hack, canonical way would be to declare it in options in profiles options objects
  and we would not like to have this SpecSource after all, just a SourceRecipe and SourceReady


we should have SpecRecipe and SpecReady
  SpecRecipe should contain ProfileRecipes and SpecReady should contain ProfilesReady


spec should also accept top level sources object, and it should work as if we're composing multiple specs


so we would like to have 2 types of sources structs: SpecSource and ProfileSource with Recipe and Ready


currently in configs there is hardcoded root, and home, this is not going to be controlled via ProfileSourceReady.DirectoryPath

sourceRecipe should also get a DirectoryPath field, defaults to XDG location where source is going to be cloned to (if remote)



ProfileReady should not get Host field, nor OperationRecipes
  it should have OperationsReady

- since the core object of config is now options lets rename the module to options


- i want to resign from the plugins concept completely check che code if there are references to plugins anywhere, in core code and in test code

- i want you to take a look at changes from this branch to main and simplify, reduce code duplication, reduce amount of code, follow best practices and use modern go syntax.

- i want you to find functions that are not methods but should be methods, e.g. when working with only single struct



--- id like to implement tests for these scenarios: (if not yet present)
1. autodiscover, execIf at top of che.yml
2. include 2 specs in che.yml and it have own profiles
    both with exclude and include profiles
    2.1 validateSpec configured on includedSpec level
      2.1.2 some other options included ad includeSpec level
      - i want to add a test where multiple che.yml specs are used and included ones define their workingDirectory via options
3. include remote profile + override some of it's options
4. different dir structure other than root/ and HOME/

--- mkdirs in che contain top level - files: id like to change it to directories:
  operations are named simply link: copy: etc. i want to have verbNoun naming so:
    makeDirs:
    makeLinks:
    makeCopies:
    runServices:

--- i dont like this Host struct, can we have a module with FS operations we do and use these FS operations in the OperationsReady as private methods?

--- id like to configure che via it's config file written in  $XDG_CONFIG_HOME, it should be similar to top level spec options object, id like to also create test for this with real config file

--- id like che to be fully XDG compliant with - it should honor XDG vars, and allow for overriding via own envs
  i want to add this to documentation, i want to document XDG envs and CHE envs that override XDG envs for che

--- each function in che must have descriptive name with at least one verb and noun

I want you to take a look at the prose and documentation in this
  repository and make it concise, abrupt, terse, remove duplicated
  information and optimize for highly technical audience. Aggresively. Make
  it concise, abrupt, terse and use specific standard industry known
  wording.


Next:

--- i want to use LiteSQL or similar and have ProfileDone OperationDone etc. so i can record all operations being done to the OS.
  i want to record type of operation (e.g. update, create, noop), datetime, and previous and next object
  lets also have revertOperation for each operation (on OperationDone)
    and it would undo the operation so e.g. makeLinks revert would remove the links, makeCopies would delete copies etc.
      it should consult the database for items currently installed e.g. copy may have been done, but it could be absent from repo in this case it would be removed
        by default it should not just remove files, but it should look for correct backup file and restore it

--- i want to resign from git as a source, and use a database for source of pruning broken links
      i also want to remove files that che copied and not present anymore in it
      and same behavior for rendered templates


--- expose telemetry che

- HOME is currently hardcoded with $HOME dest, get rid of this behavior


need automatic dependency bumps
