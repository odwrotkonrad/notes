# 2026-07-21

## Next
- go over che e2e log and see what can be improved

## Current

## Done
- makefile completion improvements
- later should be after done in notes, and it should be named ## Later Maybe
- bug with cls
- when i run git commit and im on main first commit, then branch, so branch is not tmp/scratch
  and finally move commit from main so it's clean
- fuzzy in history is overkill, substr match will be better
- we should rank history completions in order:
  - match query from beginning (exact)
  - match query from beginning (case insensitive)
  - match query from anywhere (case insensitive)

## Later Maybe
- normal completion should look up makefile targets, having dir contains makefile


## Other



---


che improvements

i want to have 2 kinds of logs, for human consumption when CLI is used, and for machine consumption (e.g. when otel and prometheus are involved)

general approach would be if debug is not set we only print information about what happened
  if debug is set we also add info what is going to happend and what is not going to happen and why

I want to resign from label(sth, sth) form of log

e.g. in discover-profiles in non debug important information to show is:
  che spec path
  remotes initialized/updated (and which) and that they landed in cache (abbreviate path), one per remote
  profiles discovered with their ops
    and what changes are going to be made for each op (delta only)
    and each profile working directory

in debug additional info to be shown:
  config (but only delta from defaults, do not show full)
    there should be che subcommand che print-config delta, che print-config all to output this information

  profiles that failed discovery with reason (no ops needed)

in CLI, there is no need for each log to be in one line
  our goal is to make it readable for a user, we can use headings, indentation etc.


^ write gherkin scenarios for these



---
divide configs into profiles
  shell config into auto.d

  profiles:
    code-agents
      claude, coding instructions/templates, git scripts, ccstatusline, codex

    observability
      host
      client

    shell
      che, git, kitty, lefthook, homebrew
      aws
      azure
      gcloud


I want to divide my configs repo into tree of che specs and profiles
I want to group the configs into areas and then into tools e.g.
  obserability/grafana /prometheus etc.
    i want config that connects pieces together at a containing profile

  each tool that contain a configuration should have it's install script contained and a configuration
  each tool that do not contain configuration but is installed can be at a top level profile
  we should have a place for tools to configure shell, so other profiles can inject config into auto.d that zsh will load, and to dir to install completions

  each config should provide profile with supported destination (macos/linux/virt/host) or if it can be loaded on multiple some common name

  I want to use che include refs capability with local paths, so there is sort of plugin tree

  i want these areas, with related tools:
    coding-agents
      codex
      claude
    observability
      prometheus
      otelcol
      grafana
    dev-tools
      python
        mypy
        ruff
      golang
      ruby

  our shell config contains sort of a contract with auto.d, let it be in top level config





I need to open vscode attached to a session


refactor can be big or just restructure










---
