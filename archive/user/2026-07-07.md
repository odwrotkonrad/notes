# 2026-07-07

## Next

- adopt sandbox projects
- makefile plugin
- extract more out of configs, (rename to dotfiles?)
- gitlab dashboard, i need to check repo status, recent pipeline on main, recent activity
- simplify prompt / skill
- add dirs recursively (?)
- is gh authenticated? if not i want this auth onto system with op
  - i created many tokens, clean it up
- plugin system for che
- validate glab.yml everywhere, common precommit
- extract install scripts, or config + install as a unit
- freely merge work, but produce summary to review
- maybe monorepo can reduce some pain with bumping
- maybe autobumping, renovate or similar
- maybe each session can be instance of docker from main so sessions doesn't interfere with each other
- move more from configs to different repos
- notes could be source of well formatted document each day
- somehow i should declare dependencies between repos so it should trigger pipelines one after another
- add linters to everything
  - makefile?
  - dockerfile
  - zsh
  - gitlab ci yml
- publish schema for che
  - and all envs available
  - and CLI usage
- assist with notes
  - each note is a day from template, inherits next, todo, drops done, make archives old notes to archive

## Current

## Later

## Done
- all projects to conventions
- puiblish binaries + use goreleaser for go packages
- CI image
  - preinitialize terraform providers
- reduce version duplication in ci images
- repo index
- tree init for subgroup

## Maybe No
- no need for MRs in notes repo, push to main
- if docgen fails in repo it could change these files (maybe)


## Other

### random

ship all
verify terraform providers are used







i want to see it with full perms


### plans
---
I want to implement a significant change.


render-files
  init subgroup, recursively
    start direct parents to repositories and generate repo index, with relative path + purpose
    it's recursive so parent of parent that contain repos will include its index, but this grandparent also can have own repos

wire to che
  probably we will need workspace repository, use env for workspace dir, and execute these rendering via che
  we can also move a gitlab install script out of configs to this repo to have gitlab init projects + repo index rendering,
  follow the convention so repo index is generated in assets/data , and there are templates that generate AGENTS.md and CLAUDE.md in subdir


---
I want to make some changes to my repos

I want each of repository to contain
	docs/assets/docs-agents/purpose.md
	and
	docs/assets/docs-human/purpose.md

	this purpose should be included in AGENTS.md, CLAUDE.md and README.md at a top

Purpose must state:
# why it exists? (the need, creator intention)
# what goals can be achieved with it? (high level)
# how it can be used? (interface, makefile doc)
# future direction of development (how should it look like)

Stage 1:
since this is a convention let's put this purpose in new conventions repo
also put a commenting convention in this new conventions repo
also put makefile convention in this new repo

Stage 2:
we will figure it out later, let's focus on new conventions repo

this is a convention, same as commenting convention, let's have repo conventions

---
I want to have user-mcp that will contain all my prompts

I dont want to repeat my writing e.g. simplify use best practices etc.



i kinda want to run make from workspace


notes repo (private)

I want to publish binaries for my go packages
i want to have base CI image with che etc.

---
I want to make changes to my notes repo


I want to have makefile targets:

- init-day
  it should initialize next note in user/<date>.md
  it should keep only 3 recent notes in user/ - so 3 recent notes are there
  it should move Next from previous note to the initialized one, same with current, it should not move done
  older notes are moved to archive/user/

I want to have this note structure each day in user:

## Next

## Current

## Done

## Other

it should be a snippet or a template

I want to have a skill for agent that will put a summary of most recent note / or note specified by argument,
it should prepare a note for archiving, it should correct grammar, ask user for intent for notekeeping.
workflow is basically ask some questions, prepare note, ask if user is happy, if not ask more questions and so on

it should also be a makefile target
