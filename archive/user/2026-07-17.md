# 2026-07-17

## Next

## Current

## Later

## Done

## Other

cd /
  should work

cd completion
  should also hint ~ dirs

if / is a dir, it should be / not //

investigate completion context for cmds that hint dirs, files and both


git log -<TAB> doesnt work

these merge commits

---
searching within stack could be useful but not for everyone
  let's make these Stack */* and Stack */*/* groups configurable so i can easily disable them

  and disable them

when i type cd ~<TAB> i want to see only one group: "## ~*"
---

I want to document all configuration options of files/deirs completions somehwere in spec dir
PS1 have issue:
  if dir is a named directory e.g. it's ~u_configs the PS1 can show as:
  ~/assets/spec
    where absolute path is /Users/ko/projects/gitlab/konradodwrot/configs/assets/spec
  it should be not taking these named dirs as "~"
  generally i do not want to see named dirs aliases in PS1, they should render as normal paths, unless it's home dir "~"
spec


_deep -> _deep_files

file-patterns
globbed-files


default completer for empty prompt:
  i like the completer but i want to configure max items per group similar to what is in _files_deep
  6 items for each group

  add behavior in zsh_completions
    zsh_completions should be a dir with subdirs, but with additional behavior.md file in it


for command completion i want to configure additional directories and groups
  configurable so i can define group and dirs to search for scripts
  i want to add dir with my scripts there


if PS1 is abbrebiated like this:
  /p/g/k/c/root/usr
    the path segment that is git repo root
      should not be abbreviated

for every executable file id like not to define alias suffix
  so shell executes the file instead of opening it

having i use my git scripts interactively in a foreground, id like to have log that will tell me the progress
  e.g. git-commit-upsert.zsh

for history completion id like to resign from fzf and implement it using zsh native completion system
  i want these history items to be grouped within 2 groups: within last week # last week and # last month and # older


i want to improve git-mr-upsert.zsh script
  during this script mrs for current repo should be checked and if they contain commits that are included in this branch and not yet in main it should be:
    updated if branch is the same
    closed/new opened if branch is different




  else it should create MR

--- current:
drop run-* from makefile
lefthook with no file list
