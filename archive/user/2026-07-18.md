# 2026-07-18

## Next
- che plugins


## Current
- che latest
- history group completion in cmd / file/dirs
- makefile includes at ancestor dirs

## Later

## Done

## Other


in tab completion
  i want to have a group with history completion capped to 2 items at the bottom

i want to have ability to invoke some makefile targets from parent dirs for some of the projects
  like make init-note
    lets start with it's parent

generate all templates everywhere, make

maybe tmux

tbh template -> render -> file could be extended
      template -> render -> repo (clone here)

i use too much effort for this che bumping..
  my own deps can be latest to reduce friction

even if i install latest when i run che --version i want to see real version
  it will work like that right?

i want to define env vars for make and set then in env.template
  define env vars for che so they get completed

cap could be dynamic

I want to change how we're loading configs in configs
  i want to run profile per profile instead of per command
  and use che all command
    i want to have lightweight version of it that doesn't run scripts
      qq: does che all support excluding commands configure option yet?



i want to see the profile being run before each subcommand


---
lets have this log system be json first, in a way that stdout/plain reuses values from the log json definition
