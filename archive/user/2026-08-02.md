# 2026-08-02

## Next
- makefile targets to ship with ai, for each repo
    on common makefile with it
- dedup, simplify, modernize skill
- extract miniminal zsh profile from configs & create minimal sandbox prof
- che improvement: git repo at root doesn't have to be requirement (for remote profiles)
- split installs into relevant profiles


## Current

## Done

## Later Maybe

## Other


I have sandbox project in this workspace
This sandbox project


i want to have a profile shell in my configs



I have bunch of configuration in configs in profiles
    I have a new requirement
    I want to have a shell profile that will install minimal deps (zsh and it's configuration. functions)

    configs can get a directory tools where each directory is a tool (e.g. zsh) it contains configuration files tree and a che.yml with instructions how to install this profile

    goal is to make zsh configuration easily installed without addditional overhead of installing other tools

    reuse this new profile in configs/tools/zsh/che.yml in top level configs/che.yml and dont repeat yourself


find points where zsh is coupled with other tools so we can figure sth out


---
lots of repeated paths:
        files:
          - {source: Library/LaunchDaemons/otelcol.plist.ontoHost.cp, dest: [/Library/LaunchDaemons/otelcol.plist]}
          - {source: Library/LaunchDaemons/dir-size-exporter.plist.ontoHost.cp, dest: [/Library/LaunchDaemons/dir-size-exporter.plist]}
maybe src / dest dir?


--- similarly to how we have zsh profiles i want these extracted:
    ssh
    git
    and there is workspace repo containing gitlab projects init, i want to move it to configs in tools/gitlab/projects and gitlab config should be at gitlab/* (it should offer 2 profiles , one with installng tool and configuring it (glab) and one for projects init)


--- i also want to have profiles:
    tools/claude
    tools/codex
    tools/onepassword
        host: it should install onepassword and init auth, interactively by prompting a user for a session token
        vm/container: noop
    tools/gcp
        2 profiles:
            1. install+configure
            2. auth -
               1. vm/container: it checks if gcp is authneticated if not error (it should be provided when pod is created)
               2. host: it should fetch credential from onepassword


--- i dont want to fetch secrets during sandbox build, but in runtime instead
    i want to have 2 che profiles:
        sandbox-build (when image is built)
        sandbox-runtime (when pod is created)

        everything that need auth should be in runtime

        build profile should include:
            zsh
            ssh
            git
            claude, codex, gcp

        runtime profile should check auth

        additionally sandbox project should own a build of a sandbox image


---
notice that codex and claude share prose, and there are some common utils that both can use
    e.g. git scripts

    i want to create
    llm-prose/ dir to package all llm relevant files
        prose and utility scripts

---

i want my git ## "local head" completions to be sorted in recency order

ci/oci-images-drop-dev-sandbox

---
I want to be able to commit / mr / push across many repos with changes
    not just as skill but as script

I want to create script:
    git-last-mr-pipeline-status.zsh
        that will find recent MR and check it's last pipeline status
        I want to fire warning if there are more than 1 MR


--- currently when i request a history completion and i have nothing in prompt
        bare commands are not listed because they are filtered, i do not want this behavior anymore



--- fix the MR template script
    issue: commits that superseded changes of previous commits in branch destroy quality of MR

    can we emphasize to in the MR generation script
    git-mr-upsert.zsh
    make it critically important
        to not include superseded changes by previous commits?


---
I want to create a script wrapper run-for-all-repos <script>
    so i can do:
        exec-for-all-repos.zsh git-commit-uspert.zsh
        exec-for-all-repos.zsh git-mr-upsert.zsh
            git-upsert-all-zsh

    each script should run for a repo in a background
    and at the end i want to see a report:
        repo: status (with emoji check or cross)

    i want to have options
        --include=...,...,...
        --exclude=...,...,...

    it should recursively check for repos from a directory
        -C|--chpwd= and default to pwd

    I want to have --filter option
        and it accepts arguments, so i can target repos that:
            have any changes tracked or not
            are not on main
            its branch is not up to date with remote



--- i want to add info about git pipeline status script to /user-git-ops skill
        it should be used as a measure to get pipeline information



--- we should add info about exec-for-all-repos in CLAUDE.md
        as a measure to executing command / scripts / git scripts across multiple repositories
        add examples with 1) git pipeline status, and 2) ship all















---
