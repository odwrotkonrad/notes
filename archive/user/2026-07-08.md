# 2026-07-08

## Next
- che profiles, secret / non secret ? runtime / build time
- i want to have cool profile in github / gitlab, maybe iac
- projects should have accurate desc + topics
  - i wish i could have repo.spec with desc, keywords, protected branches etc.
- che.yml profiles in configs need some love, excludes are difficult to read
- i want to publish latest version of che (changing per release)
- simplify that sandbox a little, dockerfile img seem too complex
- only code changes should trigger release (?) maybe docs are also code
- where to place workspace rules? e.g. repos created only via iac
- configs is a dir, with root, and plugins dir
- plugins could be just relative paths

- extract some of heavier installs into optional plugins
- links dest should be configurable
    rewrite/replace part of path?
- maybe another vault for OP access from sandbox
- one ssh key pair for container? or one for all?
- gomplate
- i want to reuse claude session from host to avoid redundant logins
- sometimes i will want to also run vscode along with the session, setup optional dev-container launch / attach
- grafana dashboard for sessions + pods + actions

## Current
- i need sandbox for claude

- go packages -> one repo split, with go.work and submodules

## Later

## Done
- in all repos i want to have che.yml profile "OntoRepo" (also add it to conventions)
- renderRepo and renderHost are unnecessary, we can have one render
- bookworm tag repeated as latest is not good for derived images
- che spec + envs , che docs
- need to make these gitlab pages look at least decent

## Maybe No
- move all relevant projects to konradodwrot org

## Other



- i need sandbox for claude, for now i'm gonna prepare a way to run a shell within a container with all my config

  start simple from scratch,
    base image with my config
      (configs install all)
    local cluster
    i see value in container - claude session 1:1
    i want to use kind for running k8s

  I want another repo called sandbox



I want to take another approach to building image for sandbox, we will rename the project ci-images to oci-images, and have 2 builds here: base image and sandbox-dex image base image should stay as it is right now, and in sandbox-dev we gonna clone configs and install all here, image can be published to the oci repository, i want to make sure i can pull it locally, sandbox repo will contain k8s configuration, and utils / cmds to use the sandbox



---
I want to extend che with plugin system,

I want to be able to point to repo and having it contains che.yml to reuse its profiles


plugins:
  @<giturl>::profile


let's start simple one repo you can use for testing is workspace, id like to include it in configs via new plugin system


docker build \
  --platform linux/arm64 \
  -f ci/dev-sandbox/Dockerfile \
  --build-arg BASE_IMAGE=registry.gitlab.com/konradodwrot/infra/oci-images/ci-linux:v0.0.8-arm64 \
  --build-arg CONFIGS_SHA=$CONFIGS_SHA \
  -t dev-sandbox:local-arm64 \
  --progress=plain \


make run-all

# daily driver: named session (creates or reattaches)
make run-session SESSION=s-mytopic

# list sessions (pods + disk used per home diff)
make run-session-ls

# stop pod, keep home        # resume later
make run-session-stop SESSION=s-mytopic
make run-session SESSION=s-mytopic

# delete session incl. home diff
make run-session-rm SESSION=s-mytopic

# refresh image later (new publish) then reload
make run-image-pull run-image-load

# tear the whole cluster down (all sessions + PVC die)
make run-cluster-down



---

I want to make big changes

I want to move all my go repos into one repo
  seperate modules should contain own go.mod (che, get-* modules)
  it should contain go.work at top
    che and render should be bundled together, however render will be child package (not module) within che
  i want to have seperate releases for each module
  i want to name the repo go-modules
    it should be placed at top of konradodwrot org group
  do not delete for now just prepare new home for modules
