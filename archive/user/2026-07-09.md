# 2026-07-09

## Next

- sandbox
  - make create session / attach to a session
   - run full sync on create session
  - vscode optional run / attach
  - pwd to projects/gitlab/konradodwrot
- each repo should have agent defined
  - subgroup index should have list of agents in it
  - each subgroup also should have agent defined
    - agents should not be system they should be scoped per directory / group
        // manager / orchestrator / scoping / implementation via agent invocation
- i want sessions to be persistent, at least conversation with agent

- identities / secrets
  - sandbox / agent / session own identity where possible
    - gitlab access must be limited, e.g. no deletions
    - OP valut / service account scoped to the vault
  - identities could be created via IAC, clickops is painful
    - repo with identities must be not managed by sandbox identities, (host, interactive only)
    - similar with secrets, secrets usually couple with IDs, could be one repo?
  - sandbox should have own identity
      // ideally identity per session, but common for sandbox is better than nothing

- workflows
  - workflow for implementing changes
  - extract difficulties from each session
  - workflow for periodic improvements, maintenance, simplifications
  - workflow for software updates, review changelog, what could be streamlined? are there new features?
  - MRs should not be merged by claude in sandbox
      // can be enforced via permissions, and also via instructions
      // escalation path to let me merge if needed (for multirepo implementations) // optional

- che / configs
  - work on profiles
  - configs could be refactored so there is root / and dir with plugins
  - why do we need that c buring che building? maybe we can get rid of it
  - completions




- fixes
  - why sandbox builts linux/desktop? it should be linux/cli
  - sandbox image is heavy, what takes most space?



## Current
  - now we moved to one repo for all modules - simplify, extract common pieces

## Later

## Done
- che:
    - plugins, extend che so plugin can be dir
    - one error should not stop che execution, but it should report what failed after particular step
    - i want to add log modifier to know which profile it comes from
    - i want to support plugins that paths are not just git repos but also paths to directories
    -  log: execIf - only log info if pass, if reject it should log as debug

- PS1 - prompt should include (virt) if in its not bare metal
- make completions - i don't want to see list of files but list of targets when i hit tab.


## Other


### --- PLAN: workflow for implementing changes
  scope the changes and propose plan
  make it work
  test it well
  simplify implementation
  simplify tests
  documentation


// some voices can make listener more attentive, significantly, in audio materials
// maybe communicate with agent via MR comments? why not?
    by looking at MR i want to move as quickly to convo with relevant agent / session


// maybe output styles can be used to not just tone / persona, but for additional extra all other instructions

// interactive host assistant



---
che
  - plugins exec should be after non plugins
      e.g. this plugin doesn't have mk-dirs

------------------------ BEGIN che mk-dirs               ------------------------
11:24:41.487: execIf(reject): profile desktop/macos: builtin:isOs == macos
11:24:41.487: execIf(reject): profile cli/macos: builtin:isOs == macos
11:24:41.487: execIf(pass): profile cli/linux: builtin:isOs == linux
11:24:41.495: plugin(gitlabGroup): run @https://gitlab.com/konradodwrot/workspace.git::gitlabGroup

      I'd like to not see plugin info log if there were no changes, only log if there were changes pulled, or repo was cloned


  - one error should not stop che execution, but it should report what failed after particular step
      e.g. this should continue executing:

11:24:58.069: render(create): /home/ko/.config/codex/config.toml
che: render template /home/ko/projects/gitlab/konradodwrot/configs/root/HOME/.config/gcloud/configurations/config_main.ontoHost.tpl: renderTemplate: failed to render template config_main.ontoHost.tpl: template: config_main.ontoHost.tpl:2:13: executing "config_main.ontoHost.tpl" at <op "op://ProgrammaticAccess/gcp_ko/account_email">: error calling op: op resolve "op://ProgrammaticAccess/gcp_ko/account_email": error resolving secret reference: performing a vault operation: rate limit exceeded

  - i want to add log modifier to know which profile it comes from e.g. here:

11:21:54.301: run-scripts(dry-run=all): /Users/user/.local/share/che/plugins/gitlab.com-konradodwrot-workspace/ci/zsh/scripts/bootstrap/10-clone.zsh

it should be run-scripts(dry-run=all,profile=gitlabGroup)

- i want to support plugins that paths are not just git repos but also paths to directories

  so here:

base:
  plugins:
    - ref: "@https://gitlab.com/konradodwrot/workspace.git::gitlabGroup"
      env:
        GITLAB_GROUP: konradodwrot
        HOST_DIR_GITLAB_GROUP: konradodwrot

I could do, both relative path (from this che.yml) in ref or absolute path, I'd like to also support ~, and $HOME (env expansion)



---

note:
- identities / secrets
  - sandbox / agent / session own identity where possible
    - gitlab access must be limited, e.g. no deletions
    - OP valut / service account scoped to the vault
  - identities could be created via IAC, clickops is painful
    - repo with identities must be not managed by sandbox identities, (host, interactive only)
    - similar with secrets, secrets usually couple with IDs, could be one repo?
  - sandbox should have own identity
      // ideally identity per session, but common for sandbox is better than nothing


plan:
I want to make changes.
I don't like that in sandbox the identity is the same as in



---

now take a look at the whole go-modules and please simplify the code - use modern go syntax, reduce complexity, follow best industry standard practices, make code concise and readable, reduce repetition, extract repeated code into shared library



what plugins would I have?
  llm
    claude
    codex
    ollama

  observability
    agent
    host


I'd like to rewrite my tests to be data driven, I'd like to have test cases defined in yml or json (input, expected), and use embed and testing func to run it. I want to have most if not all test cases this way, so i can simply check the test definition file to see the behavior of a testing unit


one module test with setup, do not test setup directly just the whole flow (one could say e2e)

unit test logical units of module e.g. subcommands, features (or one could say integration)

unit test functions, avoid


idk e2e and integration have some "real env" association in my mind, if it's in single module/package it's a unit test with unit redefined


testing should mock the filesystem interface, lets implement mocks for filesystem ops, lets mock interfaces and implement interfaces where missing and needed for testing


there must be packages that support cli schema gen, and config schema gen, internal docgen is odd



che:
  - root should be parametrized, and by default files could be placed in tree (?) or on top of repo
